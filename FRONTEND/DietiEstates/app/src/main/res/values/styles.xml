<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base App Theme -->
    <style name="AppTheme" parent="Theme.Material3.DayNight">
        <!-- General theme colors -->
        <item name="colorPrimary">#54B5F0</item>
        <item name="colorOnPrimary">#D9F0FF</item>
        <item name="colorSecondary">#03DAC6</item>
        <item name="android:windowBackground">@color/background_color_dark</item>

        <!-- Default Button Style -->
        <item name="materialButtonStyle">@style/BlueButton</item>
    </style>

    <!-- Button Styles -->
    <style name="BlueButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">#54B5F0</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- EditText Style -->
    <style name="EditTextStyle" parent="Widget.Material3.TextInputEditText.OutlinedBox.Dense">
        <item name="android:background">@drawable/rounded_edit_text</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textColorHint">#9E9E9E</item>
        <item name="android:padding">16dp</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <!-- Button Styles -->
    <style name="AnnullaTextView" parent="TextAppearance.Material3.DisplayMedium">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAlignment">viewStart</item>
    </style>

    <!-- DialogTheme Styles -->
    <style name="DialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="colorAccent">@color/button_color_light</item>
        <item name="android:buttonBarButtonStyle">@style/PickerButtonStyle</item>
        <item name="android:textColorPrimary">@color/background_color_dark</item>
        <item name="android:textColorSecondary">@color/background_color_dark</item>
    </style>
    <style name="PickerButtonStyle">
        <item name="backgroundTint">#FFFFFF</item>
        <item name="android:textColor">@color/background_color_dark</item>
    </style>

</resources>
